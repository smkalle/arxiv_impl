[pytest]
# Only the harness's own suite. `hidden_tests/` and `workspace/` hold generated
# files that are run by the grader with an explicit path and must never be
# collected here.
testpaths = tests
norecursedirs = hidden_tests workspace runs tasks .venv __pycache__
addopts = -q
